#include <cstdio>
#include <algorithm>
using namespace std;

int n, m, r;
int s[1000], e[1000], eff[1000];
int ans[1000000];

void solve(){
  for(int i = 0; i < n; i++){
    
  }
}

int main()
{
  scanf("%d %d %d", &n, &m, &r);
  for(int i = 0; i < m; i++){
    scanf("%d %d %d", &s[i], &e[i], &eff[i]);
  }
  solve();
  printf("%d\n", max(ans[0][0][w], ans[1][0][w]));
  return 0;
}
